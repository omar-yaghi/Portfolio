<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Omar Yaghi | Portfolio</title>
  <link href="https://fonts.googleapis.com/css2?family400;600&display=swap
  <style>
    * {
      box-sizing: border-box;
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to right, #f0f2f5, #e3e8ee);
      color: #1f1f1f;
    }

    nav {
      position: sticky;
      top: 0;
      background: #0f172a;
      padding: 1rem 2rem;
      display: flex;
      justify-content: center;
      gap: 2rem;
      z-index: 1000;
    }

    nav a {
      color: #cbd5e1;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s;
    }

    nav a:hover {
      color: #ffffff;
    }

    header {
      background: #0f172a;
      color: white;
      padding: 4rem 1rem;
      text-align: center;
    }

    header h1 {
      font-size: 2.8rem;
      margin-bottom: 0.5rem;
    }

    header p {
      font-size: 1.2rem;
      color: #cbd5e1;
    }

    section {
      max-width: 1200px;
      margin: 3rem auto;
      padding: 0 1rem;
    }

    section h2 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 2rem;
    }

    .about p {
      max-width: 700px;
      margin: auto;
      font-size: 1.1rem;
      line-height: 1.6;
      text-align: center;
    }

    .project-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }

    .project-card {
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      padding: 1.5rem;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 0.6s forwards;
    }

    .project-card:nth-child(2) { animation-delay: 0.2s; }
    .project-card:nth-child(3) { animation-delay: 0.4s; }

    .project-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
    }

    .project-card h3 {
      margin-top: 0;
      font-size: 1.3rem;
    }

    .project-card p {
      font-size: 0.95rem;
      color: #444;
    }

    .project-card a {
      display: inline-block;
      margin-top: 1rem;
      color: #2563eb;
      text-decoration: none;
      font-weight: 600;
    }

    .contact form {
      max-width: 600px;
      margin: auto;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .contact input, .contact textarea {
      padding: 0.8rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
    }

    .contact button {
      padding: 0.8rem;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s;
    }

    .contact button:hover {
      background: #1e40af;
    }

    footer {
      text-align: center;
      padding: 2rem 1rem;
      background: #0f172a;
      color: #cbd5e1;
      margin-top: 3rem;
    }

    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 600px) {
      header h1 {
        font-size: 2rem;
      }

      section h2 {
        font-size: 1.5rem;
      }

      nav {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>

  <nav>
    #aboutAbout</a>
    #projectsProjects</a>
    #contactContact</a>
  </nav>

  <header>
    <h1>Omar Yaghi</h1>
    <p>Future Software Engineer | Project Showcase</p>
  </header>

  <section id="about" class="about">
    <h2>About Me</h2>
    <p>
      I'm Omar Yaghi, an aspiring software engineer passionate about building modern, user-friendly web applications.
      I enjoy learning new technologies, solving real-world problems, and collaborating on creative projects.
    </p>
  </section>

  <section id="projects" class="projects">
    <h2>My Projects</h2>
    <div class="project-grid">
      <div class="project-card">
        <h3>Project One</h3>
        <p>A modern web app built with React and Firebase. Handles real-time data and user authentication.</p>
        #View Project</a>
      </div>
      <div class="project-card">
        <h3>Project Two</h3>
        <p>Mobile-first portfolio site using HTML, CSS, and JavaScript. Fully responsive and animated.</p>
        #View Project</a>
      </div>
      <div class="project-card">
        <h3>Project Three</h3>
        <p>Data visualization dashboard using D3.js and Chart.js. Interactive charts and filters included.</p>
        #View Project</a>
      </div>
    </div>
  </section>

  <section id="contact" class="contact">
    <h2>Contact Me</h2>
    <form>
      <input type="text" placeholder="Your Name" required />
      <input type="email" placeholder="Your Email" required />
      <textarea rows="5" placeholder="Your Message" required></textarea>
      <button type="submit">Send Message</button>
    </form>
  </section>

  <footer>
    <p>© 2025 Omar Yaghi. All rights reserved.</p>
  </footer>

</body>
</html>
